import React, {useState,useEffect} from 'react';
function Square({cloumnClick}) {
    const [square, setSquare] = useState();
    useEffect(() => {
       
    },[])

  return (
    <div className="Square">
    </div>
  );
}

export default Square;
